[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# React Router (SPA): rotas como /clientes, /metricas etc.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
